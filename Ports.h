#include <stdint.h>

// ADC0 init Routine
void ADC0_Init(void);

// Busy Wait ADC0 Read
uint32_t ADC0_In(void);

// ADC1 init Routine
void ADC1_Init(void);

// Busy Wait ADC1 Read
uint32_t ADC1_In(void);